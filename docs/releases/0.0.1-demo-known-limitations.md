---
version: 0.0.1-demo
includes_security_dispositions: []
---

# Known Limitations — v0.0.1-demo

## Unsigned tag (follow-up required)

The release tag `v0.0.1-demo` is **annotated**, not **signed**. `art-release-readiness` standing rule is signed tags; annotated is permitted only as a fallback when GPG is unavailable in the implementation environment, with this follow-up recorded.

**Follow-up:** Once GPG is configured on the maintainer's machine (or in CI), re-tag with:

```bash
git tag -d v0.0.1-demo
git tag -s v0.0.1-demo HEAD -m "SpecFlow v3.1 demo release"
git push origin v0.0.1-demo --force
git tag -v v0.0.1-demo   # must succeed
```

This follow-up will be tracked as a candidate change in the next `/retrospective` (per `art-retrospective-cadence`). The first retrospective after v3.1 lands should review whether GPG should be a documented prerequisite for v3.2 releases or whether the fallback path is the long-term policy.

## No other findings

Security review for v0.0.1-demo is empty (0 findings); no accepted or deferred SEC-NNN entries exist. Nothing else to document here.
