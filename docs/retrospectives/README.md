# Retrospectives

Periodic factory retrospectives. Naming: `YYYY-MM-DD-<topic>.md`. Human-triggered via `/retrospective` (article `art-retrospective-cadence`). Produces *proposals*, not direct changes.
