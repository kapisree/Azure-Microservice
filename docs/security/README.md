# Security

Per-release security review and disposition. Naming: `<version>-review.md` and `<version>-disposition.md`. Severity rubric: CVSS v4.0 (article `art-threat-driven-security`). Per-finding frontmatter required: see spec § Artifact Schemas.
