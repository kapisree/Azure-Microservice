# Security Disposition — v0.1.0 (Claims Status API)

Solo-team provisional severities (per `art-threat-driven-security`): the
author (this session) recorded `severity` provisionally for all five
findings below. They require confirmation or downgrade by a second
reviewer; until then treat severities as provisional, not final.

### SEC-001
```yaml
---
id: SEC-001
status: accepted
owner: kapisree
expiry: 2026-12-31
rationale: "Already accepted under ADR-004 for v1: unauthenticated read access to synthetic claim data is the spec's deliberately chosen v1 boundary (tb-1). Revisit before any real claimant data is substituted in, or before this expiry, whichever is sooner."
---
```

### SEC-002
```yaml
---
id: SEC-002
status: accepted
owner: kapisree
expiry: 2026-12-31
rationale: "Out of scope per the spec's Won't Have (no rate limiting in v1); Kubernetes resource requests/limits already bound the DoS blast radius to the pod's own allocation, per the threat model's own mitigation note."
---
```

### SEC-003
```yaml
---
id: SEC-003
status: deferred
owner: kapisree
expiry: 2026-09-30
rationale: "Digest pinning requires resolving the current mcr.microsoft.com manifest digest against a live registry, not available in this offline review session. Low exploitability (requires Microsoft's own registry to be compromised or a targeted MITM at build time); deferred to a follow-up PR rather than guessing a digest value here."
---
```

### SEC-004
```yaml
---
id: SEC-004
status: deferred
owner: kapisree
expiry: 2026-09-30
rationale: "Fixing requires a product decision on which severities should fail the build (e.g. critical/high only vs. any), which is a policy call beyond this review's scope. Deferred to a follow-up PR that also updates the workflow's documented gating behavior."
---
```

### SEC-005
```yaml
---
id: SEC-005
status: deferred
owner: kapisree
expiry: 2026-09-30
rationale: "Requires deciding on a stable authorized IP range (CI runner egress ranges are not static) or standing up Azure AD group mappings before enabling enableAzureRBAC — both are operational decisions outside this spec's templates. Deferred to a follow-up infra PR."
---
```

## Gate check

- Critical, open: **0**
- High, open: **0**
- Medium, accepted/deferred: **4** (SEC-001, SEC-003, SEC-004, SEC-005) — under the default cap of 5
- Low: **1** (SEC-002) — does not count toward the cap, no `expiry:` required (recorded anyway for traceability)

Per `art-threat-driven-security`, SECURITY exits clean: no critical or high
finding is open, accepted, or deferred. RELEASE may proceed once a human
reviewer confirms the provisional severities above.
