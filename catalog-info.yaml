apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: claims-status-api
  description: Claims Status API — verified spec-to-code pipeline product (SpecFlow Factory)
  annotations:
    github.com/project-slug: kapisree/Azure-Microservice
  tags:
    - dotnet
    - aks
    - claims
spec:
  type: service
  lifecycle: production
  owner: group:claims-platform
  system: claims-platform
